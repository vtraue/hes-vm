sources := "src/*.c src/bytecode/*.c" 

format:
	clang-format -i --style=Google {{sources}}
tidy : format
	clang-tidy --checks='clang-analyzer-*' {{sources}}

build: tidy
	clang {{sources}} -std=c23 -g -Werror -Wall -fsanitize=address -lSDL3 -o ../out/debug.bin
run:
	../out/debug.bin
